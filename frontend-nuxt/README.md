# AI Debate Frontend

Nuxt 4 frontend for the AI Debate local app.

## Development

```bash
pnpm install --frozen-lockfile
pnpm dev
```

The frontend expects the FastAPI backend to be available at `NUXT_PUBLIC_AI_DEBATE_API_URL` and `NUXT_PUBLIC_AI_DEBATE_WS_URL`.
